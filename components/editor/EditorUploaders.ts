import { uploadFile } from "@/lib/upload/upload-client";

export const imageUploader = uploadFile;
export const videoUploader = uploadFile;
export const audioUploader = uploadFile;
export const attachesUploader = uploadFile;